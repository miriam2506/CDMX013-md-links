/* const promise = new Promise((resolve, reject) => {
  const miNumero = 10

  if (miNumero > 10) {
    resolve('si es mayor')
  } else {
    (error reject('no es mayor')) 
  }
})
console.log(promise)
promise.then(console.log).catch(console.log)
+/